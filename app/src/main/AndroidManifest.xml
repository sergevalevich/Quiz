<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.valevich.diapro"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".DiaApplication"
        android:theme="@style/AppTheme">

        <activity android:name=".firebase.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:host="friendlychat.firebase.google.com"
                    android:scheme="http"
                    android:pathPrefix="/message"
                    />
            </intent-filter>
        </activity>

        <activity android:name=".firebase.SignInActivity" />

        <activity android:name=".flows.navigation.NavigationActivity">
        </activity>

        <activity android:name=".flows.notes.view.add.NewNoteActivity">

        </activity>

        <activity android:name=".flows.tags.view.TagsActivity">

        </activity>
    </application>

</manifest>